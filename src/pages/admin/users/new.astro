---
export const prerender = false;

import Layout from '../../../layouts/Layout.astro';

const error = Astro.url.searchParams.get('error');
---

<Layout title="Crear cuenta de admin">
	<main class="new-user">
		<a href="/admin">&larr; Volver</a>
		<form action="/api/admin/create-user" method="post">
			<h1>Crear cuenta de admin</h1>
			{error && <p class="error">{error === 'missing_fields' ? 'Ingresa correo y contraseña.' : error}</p>}
			<label for="email">Correo</label>
			<input type="email" name="email" id="email" required autocomplete="email" />
			<label for="password">Contraseña temporal</label>
			<input type="password" name="password" id="password" required minlength="8" autocomplete="new-password" />
			<button type="submit">Crear cuenta</button>
		</form>
	</main>
</Layout>

<style>
	.new-user {
		max-width: 320px;
		margin: 2rem auto;
		padding: 0 1rem;
	}
	form {
		display: flex;
		flex-direction: column;
		gap: 0.75rem;
		margin-top: 1rem;
	}
	.error {
		color: #b91c1c;
		margin: 0;
	}
	input {
		padding: 0.5rem;
		border: 1px solid #ccc;
		border-radius: 4px;
	}
	button {
		padding: 0.6rem;
		border: none;
		border-radius: 4px;
		background: #111827;
		color: white;
		cursor: pointer;
	}
</style>
